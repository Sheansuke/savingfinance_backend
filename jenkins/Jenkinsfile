/* groovylint-disable-next-line CompileStatic */
pipeline {
    agent none
    stages {
        stage('Back-end') {
            agent {
                docker { image 'mvertes/alpine-mongo' }
            }
            steps {
                sh 'mongo image initialized'
            }
        }
    }
}

