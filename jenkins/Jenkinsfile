/* groovylint-disable-next-line CompileStatic */
pipeline {
    agent any
    stages {
        stage('build') {
            steps {
                sh 'npm install -g yarn'
                sh 'yarn install'
            }
        }

        stage('run test') {
            steps {
                sh 'yarn test'
            }
        }
    }
}
