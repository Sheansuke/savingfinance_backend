/* groovylint-disable-next-line CompileStatic */
pipeline {
    agent any
    stages {
        stage('build') {
            steps {
                sh 'yarn install'
            }
        }

        stage('run test') {
            steps {
                sh 'yarn test'
            }
        }
    }
}
